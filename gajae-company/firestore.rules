rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /chronicles/{document} {
      allow read: if true;
      allow write: if false; // Secure it after migration
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
